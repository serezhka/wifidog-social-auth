const config = {};

config.port = 80;
config.vkApiId = 0;
config.vkSecretKey = '';
config.authTime = 30; // seconds, time required to auth after clicking "Get internet access" button

module.exports = config;